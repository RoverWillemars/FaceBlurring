1. Place images in "DropImagesHere" folder
2. Install dependencies (pip install opencv-python)
3. Hit run
4. Check output folder ("BlurredOutput")
5. If face is not recognised, try changing the arguments in line 20 (1.1 & 3) to other values. Check documentation for what the arguments mean.
